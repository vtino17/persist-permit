# Security policy

Security fixes support the latest default-branch version.

Use GitHub private vulnerability reporting for bypasses, integrity failures, or
unsafe compiler behavior. Include the affected commit, sanitized batch, store,
policy, observed result, expected result, and impact.

A clean result does not prove that proposed content or provenance is truthful.
